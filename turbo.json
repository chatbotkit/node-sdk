{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".dist/**"]
    },
    "destroy": {
      "outputs": ["package.json"],
      "cache": false
    },
    "check": {},
    "test": {},
    "docs": {
      "dependsOn": ["^build"]
    }
  }
}
