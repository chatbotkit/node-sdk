query ListBots($cursor: ID, $order: String, $take: Int) {
  bots(after: $cursor, last: $take) {
    edges {
      cursor
      node {
        id
        name
        description
        createdAt
        updatedAt
      }
    }
    pageInfo {
      hasNextPage
      hasPreviousPage
      startCursor
      endCursor
    }
  }
}
